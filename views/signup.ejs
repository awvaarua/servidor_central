<% if(messageOk.length > 0){ %>
    <div class="general container">
        <div class="col-sm-6 col-sm-offset-3">
            <div class="alert alert-success">
              Usuario registrado correctamente
            </div>
        </div>
    </div>
 <% } else{ %>  
   <div class="general container">
        <div class="col-sm-6 col-sm-offset-3">

            <h1><span class="fa fa-sign-in"></span> Dar de alta un usuario</h1>

            <% if (message.length > 0) { %>
                <div class="alert alert-danger"><%= message %></div>
            <% } %>

            <!-- LOGIN FORM -->
            <form id="newuser" action="/signup" name="nuevo_usuario">
                <div class="form-group">
                    <label>Email</label>
                    <input type="text" class="form-control" name="email">
                </div>
                <div class="form-group">
                    <label>Contraseña</label>
                    <input type="password" class="form-control" name="password">
                </div>
                <div class="form-group">
                  <label for="sel1">Seleccionar tipo de cuenta</label>
                  <select class="form-control" id="sel1" name="tipo">
                    <option value="0">Normal</option>
                    <option value="1">Administrador</option>
                  </select>
                </div>
                <button type="submit" class="btn btn-warning btn-lg">Registrar usuario</button>
            </form>

            <script type="text/javascript">
                $("#newuser").submit(function(event) {
                    /* stop form from submitting normally */
                    event.preventDefault();

                    $.ajax({
                        type: 'POST',
                        url: $(this).attr('action'),
                        data: $(this).serialize(),
                        success: function(data) {
                            $('#page-wrapper').html(data);
                        }
                    }) 
                });
            </script>

        </div>
    </div>
<% } %>